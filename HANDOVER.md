# 인수인계 문서 — YOUTH LEADER (2026-09-16)

작업을 다음 사람 또는 다음 세션으로 넘기기 위한 문서다. 지금까지 결정된 것, 아직 안 정해진 것, 다음에 할 일을 모두 담았다. **코드를 건드리기 전에 이 문서를 먼저 읽는다.**

## 0. 최근 세션 (2026-09-18) — 여기부터 읽기

사용자가 "더 개선 가능한 부분"을 물어서 코드를 전부 감사했고, 그중 **Voice 탭(플레이어 의견 공간)을 먼저** 만들라는 지시를 받았다. 게임 제작보다 회복력 구축이 목적이기 때문이다.

### 이번에 한 것 (8) — 탭 씹힘 (5절 "목록을 다시 그리지 않는다" 참고)
감사 7번. `buy()`와 `give()`가 `render()`로 목록을 통째로 다시 그려서, **애들이 연타하는 바로 그 두 버튼**이 손가락 밑에서 교체되고 있었다. 이제 서명(`sig`)은 "어떤 줄이 있어야 하나"만 묻고, 숫자는 `syncRows()`가 제자리에 쓴다. 테스트가 **노드 동일성**으로 확인한다 — 같은 버튼을 5번 누르고 나서도 그 버튼 객체가 그대로 살아 있는지.

### 이번에 한 것 (7) — 링크 한 개로 퍼지게 (README "Set the site URL" 참고)
감사 6번. OG 태그, `meta description`, `theme-color`, 파비콘, 홈 화면 추가.
- **이미지를 실제로 만들었다.** `icon.svg`(직접 그린 3도형: 하늘·해·바다) → 헤드리스로 180/192/512 PNG 렌더, `og.png`는 1200×630 카드. 저작권 걸릴 게 없는 자작이고 앱 팔레트 그대로다.
- **`og:image`는 절대경로여야 한다**(상대경로는 스크레이퍼가 거부한다). 배포 주소를 모르니 `https://example.github.io/youth-leader/`를 넣어두고 index.html 맨 위에 **눈에 띄는 주석**과 README에 배포 4단계를 적었다. 테스트는 og:url과 og:image가 **서로 맞는지**만 본다 — 주소가 옳은지는 기계가 못 본다. **배포 후 자기 카톡에 한 번 붙여보고 눈으로 확인할 것.**

### 이번에 한 것 (6) — 해안 도시의 하늘, 방어 게이지, 방벽 그림 (5절 참고)
감사 4·5번.

- **4번:** R 도시는 자기 하늘을 못 치운다. 그 연기가 자기 것이 아니기 때문이다. **배출 도시에 Tech 코인을 보내면 걷힌다**(`SKY_FUND 300`, 하루 탭의 절반). 해안 도시가 자기 공기를 사는 게 아니라 **남의 감축을 사서** 얻는다는 것이 발표 논리 그대로고, 장면이 그 말을 하라고 있는 것이다. 절대 배출량 기준(속초는 옅고 울산은 짙다)은 그대로 유지했다.
- **5번:** 방어 카드에 `Today's defence: 430 left`와 가는 진행 막대를 넣었다(예전엔 `DEF_CAP 600`이 화면 어디에도 없어서 부딪혀야 알았다). 방벽은 물 위로 내려 보내 **보트 앞에** 그리고, **50% 위치에 점선 목표선 + "50% holds a wave" 라벨**을 넣었다. 넘으면 갓돌이 초록으로 바뀌고 라벨이 비켜난다.

### 이번에 한 것 (5) — 파일 분리
`index.html`(129줄) + `styles.css`(171줄) + `app.js`(717줄). 빌드 없음, Pages 그대로 동작(헤드리스로 확인). `app.js`는 **반드시 일반 스크립트로 둔다** — `type="module"`로 바꾸면 목록의 `onclick="buy(...)"`가 전부 죽는다. 테스트는 jsdom이 `<link>`·`<script src>`를 동기로 못 읽어서, 각 테스트 파일 맨 위에서 세 파일을 합쳐 넣는다. 파일 이름이 바뀌면 테스트가 즉시 전부 깨진다(일부러 그렇게 했다).

### 이번에 한 것 (4) — 방벽: 절대 안 끝나고, 절대 혼자 안 된다 (5절 "방벽" 참고)
감사 3번. **그런데 감사할 때 놓쳤던 더 큰 구멍이 같이 나왔다.**

`give('self')`가 코인 20에 +5%였다. 탭 20번 = 코인 20 = +5%인데, 직접 탭하면 20번에 +1%다. **자기 코인으로 사는 게 5배 싸다.** 실제로 돌려봤다: 600탭 → 방벽 30% + 코인 600 → 자기한테 30번 송금 → **첫날 혼자 100%.** 5절이 "자기 탭만으로는 절대 안전선에 못 간다"고 박아둔 이 게임의 중심 주장이 **출시 코드에서 이미 거짓이었다.** 감쇠만 넣었으면 하루 +150% vs 하루 −10%라 아무 의미가 없었을 것이다.

그래서 셋을 같이 넣었다: **감쇠 + 자기 자금 천장 + 외부 구호 도착.** 셋 중 하나만 빼도 나머지가 망가진다 — 천장이 없으면 감쇠가 무의미하고, 외부 구호가 없으면 정적 빌드에서 50%에 영원히 못 가서 게임이 그냥 진다.

### 이번에 한 것 (3) — 랭킹 (5절 "하루 보드" 참고)
감사 2번. **하루 점수 = 70 × 내 몫 완수율 + 30 × min(1, 오늘 보낸 코인/200)**, 최대 100. 매일 0에서 시작하고 **랭킹만** 리셋된다(누적 감축·기부·스트릭·업그레이드·방벽은 남는다). 인구는 1인 평균이라 식에서 빠지지만 `pop` 칸은 데이터 표에 넣어뒀다.

### 이번에 한 것 (2) — 오프라인 수익 (5절 "자리를 비운 시간" 참고)
감사 1번을 처리했다. `S.at`에 저장 시각을 찍고, 돌아왔을 때 그만큼 업그레이드를 돌려준다. **하루치의 절반까지만** 준다 — 다 주면 돌아왔을 때 탭할 게 없어서 매일 여는 이유가 오히려 사라진다. 백그라운드 탭(`tick()`의 큰 `dt`)도 같은 천장을 쓴다.

### 이번에 한 것 (1): Voice 탭 (5절 "Voice" 참고)
탭이 4개가 됐다. `My City | Support | Voice | Ranking`.
**채팅 금지 규칙은 안 깼다.** 플레이어끼리 서로의 글을 읽는 면이 0이다 — 투표는 선택지 4개뿐이고, 자유 의견은 기기에만 남아 팀에게 간다. 사용자가 고른 형태이고, 자유 의견 전송은 **광고 화면과 같은 자리표시자**다(구글 폼 주소가 정해지면 `sendNote()` 한 함수만 바꾼다).

### 아직 안 한 것: 감사에서 나온 나머지 (우선순위 확정됨)
아래 순서로 가기로 사용자와 합의했다. **7절보다 이게 우선이다.**

1. ~~오프라인 수익이 없다.~~ **끝남** (위 참고).
2. ~~랭킹이 깨져 있다.~~ **끝남** (위 참고).
3. ~~방벽이 깎이지 않는다.~~ **끝남** (위 참고).
4. ~~R 도시는 스모그를 영원히 못 걷는다.~~ **끝남** (위 참고).
5. ~~방어 모드에 오늘치 게이지가 없다 / 방벽이 4px 선이다.~~ **끝남** (위 참고).
6. ~~og:title/description/image가 없다.~~ **끝남** (위 참고). **단, 배포 후 주소 교체가 남았다 — README 4단계.**
7. ~~탭 씹힘이 두 군데 남아 있다.~~ **끝남** (위 참고).
8. `.chip`에 `cursor:pointer`만 있고 핸들러가 없다(죽은 어포던스). 시작 후 도시를 바꿀 방법이 없고, 게이트에서 다른 도시를 고르면 `S.uid=uid()`로 저장이 조용히 날아간다(경고 없음).
9. B 도시는 하루 1200탭(600+600), 나머지는 600탭이라 코인 수급이 2배다.
10. "Play" 더블탭 시 `setInterval`이 두 벌 뜰 수 있다(가드 없음). 속도 2배. 한 줄.
11. CLAUDE.md의 스타일 규칙(`0F4C9C`/`F8FAFC`/`E4ECF5`/Poppins)과 코드(`#0E2A47`/`#EEF3F5`/Pretendard)가 다르다. 발표 슬라이드와 앱 색이 안 맞는다 — 한쪽으로 맞출 것.

**커밋 안 됨.** 자동 테스트 493개는 통과하고 CLAUDE.md의 수동 점검도 헤드리스로 돌렸지만(탭 30 → 코인 30, 30코인 구매 거부/130코인 구매 성공, 송금 20, 랭킹 12줄), **실제 폰에서는 아직 안 돌려봤다.**

## 0-1. 이전 세션 (2026-09-16)

사용자가 번호 붙은 요청으로 작업을 준다(1, 2, 3 …). 이번 세션에서 한 것:

1. **스페이스바 = 탭** (노트북용). My City 탭에서만, 키를 누르고 있으면 반복 안 됨(오토클릭 방지), 입력칸에서는 무시. 포커스된 버튼이 스페이스로 눌리는 것도 막음. 탭 로직은 `tap(x,y)` 하나로 합쳐짐.
2. **리워드 광고 자리표시자** — 5절 "리워드 광고" 참고. 사용자가 "광고가 별로면 일시 성능 향상 아이템"을 대안으로 줬는데, 광고 수익 = 기부가 해결책의 핵심(3절)이라 광고로 갔다. **부스트 아이템은 만들지 않았다.**
3. **도시별 풍경 + 은은한 애니메이션** — 5절 "도시별 풍경" 참고.

**아직 커밋 안 됨.** 사용자가 이 세션에서는 커밋하지 말라고 했다. 커밋 전에 CLAUDE.md의 수동 점검(탭 30번, 업그레이드 구매, 코인 송금, 랭킹 확인)을 해야 한다.

**화면 확인 방법 (Chrome 확장이 연결 안 될 때):** 헤드리스 Chrome으로 스크린샷을 찍는다. `index.html`을 스크래치 폴더에 복사하고, `</body>` 앞에 `sel.value=<도시코드>; $("#startBtn").click()`을 넣어 바로 게임이 시작되게 한 뒤 `chrome.exe --headless=new --window-size=420,420 --virtual-time-budget=1500 --screenshot=...`로 찍는다. 380px 폭은 `<iframe style="width:380px">` 두 개를 나란히 둔 페이지로 본다(헤드리스 창은 좁게 줄어들지 않는다). file:// 주소는 Chrome 확장이 막고, Python PIL은 설치돼 있지 않다.

## 1. 배경

- 2026년 10월 세계 쓰나미의 날 UN 고교생 서밋, 한국팀(현대청운고 SeYeon, MinChae, JiHun).
- 배정 테마 B: Building Resilience Before Disaster Strikes.
- 발표 제목: No More Natural Disasters: Building Resilience from the Increased Scale of Disasters Caused by Human Activity.
- 제출 서류(Attachment 4)와 발표 슬라이드는 완성됨. 이 저장소는 그 발표에서 제안한 앱을 실제로 만드는 곳이다.

## 2. 문제의식 (발표 논리, 앱 카피에 그대로 반영)

1. 더 이상 자연재해는 없다. 탄소 → 바다 온난화 → 해수면 상승 → 모든 파도가 커진다. 재해의 규모는 인간이 정한다.
2. 울산(공업도시)에서 본 문제는 공장 수가 아니라 돈의 흐름이다. 기업과 강대국 정부는 법이 허용하는 선까지만 지키고 그 안에서 이윤을 극대화하며, 그 이윤은 재해 위험을 줄이거나 회복력을 만드는 데로 돌아가지 않는다.
3. 배출국에 기후세를 강제할 수 없고, 아직 일어나지 않은 남의 나라 재해에 세금을 쓰려는 사람도 없다.
4. 필요한 것 두 가지: 이 재해가 우리가 만든 것임을 아는 사람, 그리고 반대 방향으로 흐르는 돈.

## 3. 해결책 (확정)

캐주얼 클릭 게임. 탭 = 배출 감축(화면의 스모그가 걷힘) = 코인. 코인은 자기 지역에서 못 쓰고 다른 지역으로만 보낸다. 광고 수익이 실제 돈이 되어, 플레이어가 고른 지역의 지자체 또는 환경 NGO에 기부된다. 강제가 아니라 자발로 돈이 흐르게 만드는 것이 목적.

### 사용자가 직접 정한 구조
- 접속 시 IP 기반 국가(지역) 설정, 기기 UID로 이전 데이터 인식
- 지역 분류: high-emission / disaster-risk / both
- 온실가스 감축 방향의 클릭 → 코인
- 코인 → 재난우려 지역 방제 지원 또는 배출 지역 기술 지원
- 광고 수익 공개 후 플레이어가 고른 지역에 기부
- Global Click Day(동시 참여일) 개념 유지

### 필수 조건 8가지 (사용자 지정)
보안 / 국가(지역) 순위 / 단순 / 지속성(유행 아님) / 중독성 / 재난 교육 / 모든 국가 접근 가능·비정치적 / 지역 커뮤니티 형성

### 폐기된 아이디어 (다시 제안하지 말 것)
방벽 쌓기 클리커, Popcat식 무한 클릭 순위, Downstream(상류·하류), Inspector(감독관), 세대 상속, 결의안 스와이프, 하루 1회 Wordle식 훈련(DRILL 60). 사용자가 모두 반려함.

## 4. 실행 계획 (Attachment 4 최종본 기준)

- 앱은 팀이 직접 만든다. 외부 개발자·학교 코딩 동아리·교사 협력 없음.
- 첫 플레이어는 급우, 그 다음 한국 다른 도시의 학교들. 울산만 보드에 있으면 안 됨.
- 광고 수익은 플레이어가 고른 도시의 지자체 또는 환경 NGO에 기부.
- 단계: 웹 버전 1개월 제작 → 학교에서 2주 테스트 → 2027년 2월 한국 도시 파일럿, 광고 수익 시작 → 매달 수익·기부 내역 공개 시트 → "돈이 실제로 방제를 만들었나?" 검증 → 예이면 서밋 파트너 학교로 링크 공개, 글로벌 확장(도시 → 국가).
- 1년 목표: 한국 플레이어 1,000명, 기부 1건 완료.

미결: 파일럿 날짜가 문서에는 2월 2027로 되어 있으나 세계 쓰나미의 날은 11월 5일이다. 둘 중 하나로 정해야 함.

## 5. 현재 코드 상태 (index.html, 887줄)

세 파일, 빌드 없음, 런타임 의존성 없음, GitHub Pages에서 그대로 동작. (`test/`와 `package.json`은 개발용이고 앱과 무관하다. 8절 참고.)

### 화면 구조

```
상단바   YOUTH LEADER      [도시] [코인]      ← 코인은 어느 탭에서나 보임
탭   [ My City | Support | Voice | Ranking ]  ← Support에 재난 시 빨간 점
                                              Voice에 미응답 시 노란 점
My City  도시 장면 → (모드 전환) → 미터 → 티커 → 업그레이드   ← 한 페이지
```

Support / Voice / Ranking으로 가면 도시 장면도 같이 사라진다. 380px 화면에서 목록이 화면 전체를 쓰게 하려는 것이다.

### 역할이 게임을 나눈다

도시 역할(E/R/B)에 따라 **탭의 의미 자체가 다르다.**

| 역할 | 탭하면 | 미터 | 업그레이드 |
|---|---|---|---|
| E 배출 도시 | 배출 −1t, 연기 puff | Today's emissions left | 교통·태양광·풍력·클린그리드 |
| R 재난우려 도시 | 방어 +1, 블록이 벽에 쌓임 | Your shield % | 배수·경보·방재림·방파제 |
| B 둘 다 | 상단 버튼으로 전환 | 두 장 다, 활성 카드에 테두리 | 모드에 따라 교체 |

- 하루 상한: 배출 `DAY_CAP 600`, 방어 `DEF_CAP 600`. 둘 다 매일 리셋, 스트릭 유지.
- **방어 20포인트 = 방벽 1%.** 아래 "방벽" 절 참고. **이 비율들을 바꾸면 게임의 주장이 바뀐다.**
- 업그레이드 두 라인은 id가 안 겹쳐서, B 도시가 모드를 바꿔도 산 것은 남고 둘 다 오프라인에서 계속 돌아간다.
- 업그레이드 이름은 **도시 12곳 전부 현지화**(`LOCAL` 표, 60개). 비용·속도·id는 공통이고 이름만 바뀐다. 울산=산업 배출 저감/태화강 제방, 부산=항만 방재, 목포=저지대 배수, 제주=돌담. **공공사업만 쓴다. 개인 행동(버스를 타세요, 불을 끄세요)은 금지** — `test/city.test.js`가 그런 단어를 잡아낸다. `LOCAL`에 없는 지역은 일반 이름으로 폴백하므로 글로벌 모드도 안전하다.

### 재난 이벤트

- 45초마다 무작위 우려 지역에 파도. 방벽 50% 이상이면 막는다.
- 못 막으면 그 도시가 **Support 탭 최상단에 빨간 카드로 3분간 고정**되고, 그 안에 보낸 코인은 **효과 2배**(+5% → +10%). Support 탭에 빨간 점이 점멸한다. 동시 최대 3건, 같은 도시가 또 맞으면 타이머만 리셋.
- 비상 상태는 저장하지 않는다(3분짜리 실시간 이벤트). 새로고침하면 사라진다.
- 내 도시가 맞으면 코인 20% 손실 + "광고 보고 복구" → 잃은 만큼 정확히 돌려줌.

### 도시별 풍경

- `SCENES` 표에 도시 12곳의 하늘색·산·전경을 넣었다. 스카이라인은 `ind`(공업) / `metro`(대도시) / `town`(해안 마을) 세 가지이고, 도시마다 배치를 섞어서 서로 다르게 보인다.
- 도시마다 움직이는 것이 하나씩 있다: 울산 골리앗 크레인, 포항 용광로 불빛·일출, 여수 다리 조명(저녁), 인천 비행기·해상대교, 서울 강변 열차·남산형 탑, 대구 모노레일·사과밭(내륙이라 바다 없음), 부산 산비탈 주택·항만 크레인, 강릉 백두대간·해송·파도, 속초 설악형 암봉·등대, 제주 한라형 순상화산·귤나무, 통영 케이블카·굴 양식장, 목포 유달형 암산·갯벌.
- **LOCAL과 같은 규칙:** 지형과 공공 구조물만 그린다. 기업 시설, 바다 이름, 특정 건축물을 그대로 본뜬 모양은 넣지 않는다.
- R 도시는 굴뚝을 숨긴다(탭이 해안으로 가기 때문). 애니메이션은 CSS뿐이고 `prefers-reduced-motion`이면 모두 멈춘다. 애니메이션이 붙은 `<g>`에는 `transform` 속성을 달지 않는다(CSS가 덮어쓴다). 그래서 `at()`이 그룹을 두 겹으로 감싼다.
- `SCENES`에 없는 지역(글로벌 모드)은 예전의 기본 도시 그림으로 나온다.

### 리워드 광고 (임시 광고 화면)

- 광고는 **플레이어가 직접 여는 리워드형만.** 강제 전면광고 없음(청소년 서비스).
- 광고 화면(`#ad`)은 자리표시자다. 5초(`AD_SEC`) 뒤 보상 버튼이 열리고, 중간에 닫으면 보상 없음. 실제 광고망을 붙일 때 `watchAd()` 안만 바꾸면 된다.
- 빈도: 1분에 1회(`AD_GAP_MS`), 하루 10회(`AD_DAY`). **보여준 순간** 횟수에 들어간다. 재난 복구 광고만 1분 간격을 건너뛴다(파도는 플레이어가 고른 타이밍이 아니라서). 하루 상한은 동일.
- 코인 없이 쓰는 기능: Support 모든 줄의 `▶ Ad` = 코인 20 무료 송금(LIVE 카드면 2배 그대로), 재난 복구. 완료 1회당 장부 +$0.02(`AD_USD`, 추정치).
- 하루 탭 상한(600)은 광고로 풀지 않는다. 풀면 봇 방지와 "혼자서는 50% 못 간다"는 논리가 깨진다.

### 해안 도시의 하늘 (`SKY_FUND`)

- **R 도시는 자기 하늘을 못 치운다.** def 모드 탭은 `S.tons`를 안 건드리고 UP_CUT도 안 보이니, 예전엔 `S.tons`가 base에 영원히 고정이었다 — 부산은 opacity 0.475가 붙박이였고, 파일럿 12곳 중 6곳이 "사람들이 기억하는 단 하나"인 스모그 걷힘을 한 번도 못 봤다.
- **지금은 배출 도시에 보낸 Tech 코인이 걷는다.** `S.techToday / SKY_FUND(300)`만큼 걷히고, 300은 하루 탭(600)의 절반이라 하늘을 다 걷고도 자기 방벽에 쓸 게 남는다.
- **Tech 송금은 역할 E 도시에만 있다.** B 도시(울산·포항·여수)는 Support에서 Build(방벽)로 뜨지 실 Tech가 아니다. 그래서 `give()`에서도 techToday가 안 오른다. 해안 플레이어의 Tech 대상은 인천·서울·대구 셋이다.
- **절대 배출량 기준은 그대로다.** 걷히는 양만 역할이 정하고, 짙기의 상한은 여전히 `min(1, base/DAY_CAP)*0.95`다 — 속초는 옅게, 울산은 짙게. 5절 아래 "그 밖에" 참고.
- **매일 아침 다시 낀다.** `techToday`가 0으로 리셋된다. 배출은 매일 다시 나오니까.
- 보낼 때마다 티커에 `Seoul is cutting. Your sky clears with theirs: 53% of today's smoke funded.`가 뜨고, 다 채우면 문구가 *"you did not clean it, you paid for it"*으로 바뀐다.

### 방벽 (절대 안 끝나고, 절대 혼자 안 된다)

이 게임이 주장하는 것 전부가 이 세 숫자에 들어 있다. **바꾸기 전에 3절을 다시 읽을 것.**

- **`SAFE 50`** — 이 이상이면 파도를 막는다.
- **`SELF_MAX 40`** — 자기 손과 자기 코인은 여기서 멈춘다. **50%까지의 마지막 구간은 다른 도시의 구호로만 넘는다.**
- **`SHIELD_DECAY 10`** — 매일 아침 10포인트씩 내려간다. 바다는 안 멈춘다.
- **`shieldUp(pct, others)` 하나로만 방벽이 바뀐다.** `others`가 true인 것(= 외부 구호)만 `SELF_MAX`를 넘는다. 문을 하나로 만들어야 천장을 우회할 길이 안 생긴다.

**왜 천장을 넣었나 — 원래 주장이 코드에서 거짓이었다.** 예전엔 `give('self')`가 코인 20에 +5%였는데 직접 탭은 20번에 +1%였다. 자기 코인이 5배 싸서, 600탭 → 코인 600 → 자기한테 30번 송금 = **첫날 혼자 100%**였다. 지금은 600탭으로 30%, 남은 코인 전부 부어도 40%에서 멈춘다(실제 실행으로 확인). **하루 종일 혼자 해도 파도를 못 막는다.**

- 천장에 닿으면 Support의 자기 줄에서 **버튼이 아예 사라진다.** 안 그러면 안 오르는 방벽에 코인을 계속 받아먹고, `▶ Ad`는 하루 10개 중 하나를 보상 없이 태운다. `give()` 자체에도 가드가 있다.
- 미터 라벨도 `Your shield · yours stops at 40%`로 바뀐다. 탭이 왜 더 이상 % 를 안 올리는지 그 숫자 옆에서 설명해야 한다.
- **탭 자체는 천장 위에서도 계속 코인을 주고 하루 보드 점수에도 들어간다.** 천장은 방벽에 걸린 것이지 하루 몫에 걸린 것이 아니다.
- **감쇠는 방벽에만 있다.** 누적 감축·기부·업그레이드는 안 깎인다. 지나간 일은 안 지나간 일이 되지 않는다.
- 50%를 유지하려면 하루 10포인트가 필요하고, 그건 **바깥에서 와야 한다.** 구호를 한 번 보내고 끝나는 게 아니라 계속 보낼 이유가 여기서 생긴다.

**그림 (`FOOT 288`, `RISE 0.30`):**
- 방벽은 **물 위에, `#fore`보다 뒤에 그린다.** 예전엔 `y=232`라 어두운 지면 띠(210–230)에 붙어서 해안선의 일부로 읽혔고, 목표선은 건물 위에 얹혔다. 그리고 `#fore`보다 먼저 그리면 **보트가 방벽 위에 올라앉는다.** 지금은 방파제고 배들이 그 뒤에 있다.
- **0%에서도 받침이 보인다**(최소 높이 6, 예전 4px 실선). 올릴 게 없는데 "Tap the shore to raise the seawall"이라고 하면 안 된다.
- **50% 자리에 점선 목표선 + `50% holds a wave` 라벨.** 이 게임이 다투는 그 선을 그림에 실제로 그려둔 것이다. 넘으면 갓돌이 초록(`#8FE3A0`)으로 바뀌고 라벨은 비켜난다.
- 라벨 `x`는 24다. 화면이 넓고 낮으면 SVG가 좌우를 잘라내서 `x=10`은 잘렸다.

### 목록을 다시 그리지 않는다

**규칙: `sig`는 "어떤 줄이 있어야 하나"만 묻는다. "그 줄이 뭐라고 쓰여 있나"는 절대 안 묻는다.**

- 값이 바뀌는 건 전부 `syncRows()`가 이미 화면에 있는 노드에 **써 넣는다** — 업그레이드 가격과 ×N, Support의 방벽·클린업 숫자, 자기 방벽 %, 비상 카드의 방벽, 그리고 `affordable()`의 회색 처리.
- `render()`는 강제 재생성, **`refresh()`는 구조가 바뀌었을 때만** 재생성한다. `buy()`·`give()`·`inbound()`는 전부 `refresh()`를 쓴다.
- **`buy(id)`는 가격을 인자로 안 받는다.** 버튼을 다시 안 그리니까 버튼에 박힌 가격은 두 번째 구매부터 낡은 값이 된다. 안에서 계산한다.
- `give()`에서 `worldStamp++`를 **뺐다.** 줄 구성이 안 바뀌는데 그걸 올리면 1초 뒤 틱에서 목록이 통째로 다시 그려진다.
- 재생성이 **맞는** 경우: 모드 전환(사다리가 통째로 바뀜), 파도 발생(빨간 줄을 맨 위에 고정), 자기 방벽이 `SELF_MAX`를 넘음(버튼이 사라져야 함), Ranking(순서가 바뀌고 버튼이 없음).
- `×N` 태그는 0일 때 `hidden`만이 아니라 **텍스트를 비운다.** hidden이어도 `textContent`에는 잡혀서 줄 이름을 글자로 읽는 곳이 깨진다.
- 테스트는 문구가 아니라 **노드 동일성**으로 본다(`el.isConnected`, `btn===다시 찾은 버튼`). 그래야 "안 씹힌다"를 진짜로 검사하는 것이다.

### 방어 카드의 하루 게이지

- `Today's defence: 430 left` + 가는 막대(`.bar.thin`). 예전엔 shield %만 있어서 `DEF_CAP 600`이 화면 어디에도 없었고, 상한에 **부딪혀야** 존재를 알았다.
- 방벽(성적)과 하루치(연료)는 다른 것이라 막대 두께와 색을 다르게 뒀다. 같은 측정으로 읽히면 안 된다.

### 외부 구호 도착 (`inbound()`)

- 45초마다(재난 타이머와 같이) 다른 도시가 나에게 구호를 보낼 수 있다. +5%, 누가 보냈는지 이름이 뜬다.
- **확률이 오늘 내가 보낸 양에 따라 오른다:** `0.1 + min(0.4, S.gaveToday/500)`. 아무것도 안 보낸 도시도 10%는 받고, 많이 보낸 도시는 50%까지 간다. **주는 도시가 받는다. 강제하는 사람은 없다** — 3절의 "강제가 아니라 자발"이 딱 이 한 줄이다.
- 50%를 넘는 순간 문구가 달라진다: *"You could not have got here alone."* 발표 전체가 말하려는 순간이고, **이전까지 플레이어는 이걸 한 번도 겪어본 적이 없었다.**
- 자기 도시에서는 안 오고, 방벽이 없는 E 도시는 안 받는다.
- **시뮬레이션이다.** WORLD와 같은 급의 가짜다(5절 "안 되는 것" 참고).

### 하루 보드 (랭킹)

- **하루 점수 = `DUTY 70` × 내 몫 완수율 + `RELIEF 30` × min(1, 오늘 보낸 코인 / `RELIEF_FULL 200`).** 최대 100. `dayScore()` 하나에 다 들어 있다.
- **내 몫**은 역할이 정한다. E는 `(하루치 − 남은 t) / 하루치`, R은 `S.def / DEF_CAP`, B는 둘의 평균. **다 채우면 어느 도시든 정확히 70이다** — 대구 500 t도, 속초 방어 600도, 울산 양쪽도 같다. 역할과 규모가 점수에서 빠진다.
- **마지막 30은 집에서 못 번다.** 밖으로 나간 코인만 센다. 방벽 50% 천장이 하는 말("혼자서는 못 간다")을 점수로 한 번 더 한 것이다. 200코인 = 20코인 송금 10번 = **무료 `▶ Ad` 10번과 정확히 같다.** 코인이 하나도 없는 애도 이 절반을 채울 수 있고, 그 경로가 하필 실제 기부금이 생기는 경로다. **이 숫자를 바꾸면 광고 = 기부라는 연결이 약해진다.**
- **인구는 1인 평균으로 처리한다.** 도시 총합을 안 쓰는 것이 인구를 다루는 방법이다. 합계로 매기면 서울이 첫날부터 1위고 작은 도시는 다 그만둔다 — 4절의 "울산만 보드에 있으면 안 됨"과 정반대다. `pop` 칸은 데이터 표에 들어 있지만 **아무 데서도 안 쓴다.** 진짜 참여자 수를 셀 서버가 생기면 "이 도시 청소년 중 몇 %가 나왔나"를 곱하면 되고, 그때 데이터 이사가 아니라 식만 바꾸면 되게 해둔 것이다.
- **시뮬레이션 도시는 92점에서 막아뒀다.** 하루를 다 하고 200을 보내면 100이라 1위를 잡는다. 게임이 하라는 것을 했을 때 보드가 실제로 넘어가야 한다.
- **`rollDay()`가 `buildWorld()`를 부른다.** 예전엔 시작할 때 한 번만 불러서, 탭을 켜둔 채 자정을 넘기면 내 점수만 0이 되고 상대는 어제 것이 남았다.
- 리셋되는 것은 보드뿐이다. `S.gaveToday`는 매일 0, `S.given`(누적)은 그대로 간다.

### 자리를 비운 시간 (오프라인 수익)

- `flush()`가 저장할 때마다 `S.at`에 시각을 찍는다. 다시 열면 `S.at`부터 지금까지가 **비운 시간**이고, `awayPay(초)`가 그만큼 업그레이드를 돌려준다.
- **천장 두 개, 둘 다 의도한 것이다.**
  - 하루치를 절대 안 넘는다. 3일을 비워도 10시간 비운 것과 같다(`rollDay()`를 먼저 돌려서 하루치를 새로 깔고, 거기서 지급한다). 하루 상한이 이 게임을 습관으로 만드는 장치라 오프라인이 그걸 우회하면 안 된다.
  - **하루치의 절반(`AWAY_SHARE`)까지만.** 다 주면 돌아왔을 때 탭할 게 없다 — 앱을 여는 이유 자체가 없어진다. 남은 절반은 손이 해야 한다. *도시가 지은 것은 네가 없어도 돌아가지만, 네 몫까지 대신하지는 않는다* — 발표 논리와 같은 말이라 카피로도 쓸 수 있다.
- **백그라운드 탭도 같은 천장을 쓴다.** `tick()`의 `dt`가 60초(`AWAY_MIN`) 이상이면 그건 이름만 다른 비운 시간이다. 안 맞추면 **탭을 켜두는 쪽이 이득**이 되고, 그건 폰 배터리로 게임을 하는 짓이다.
- 1초짜리 평범한 틱은 천장이 없다. 보고 있는 동안에는 업그레이드가 하루치를 0까지 밀어도 된다.
- 돌아오면 티커에 `While you were away: 300 t cut. 300 coins waiting.`이 뜬다. 새 날이면 스트릭 문구와 한 줄로 합쳐진다.
- 1분 미만은 새로고침이지 외출이 아니다. 아무 말도 안 한다.
- `at`이 없는 옛 저장은 지급이 0이다(없던 시간을 지어내지 않는다).

### Voice (플레이어 의견 공간)

목적은 게임이 아니라 회복력이다. 이 탭은 플레이어가 자기 도시의 방제에 대해 **답하고 알려주는** 자리다.

- **위 절반: 도시별 투표.** 질문은 역할이 정한다. E 도시는 "What should Seoul cut first?", R·B 도시는 "What does Busan need most?". 선택지 4개는 **그 도시의 `LOCAL` 업그레이드 이름 그대로**다(E는 cut 줄, R·B는 def 줄) — 새 문구를 안 만들었으니 6절의 기업명·바다 이름 금지 규칙이 자동으로 지켜진다. 답하면 막대그래프로 바뀌고 자기 답에 테두리가 붙는다. 언제든 바꿀 수 있다.
- **아래 절반: 제출함.** 자유 텍스트 300자. `S.notes`에 쌓여 **기기에만** 남는다. 화면에 "다른 플레이어는 볼 수 없다"고 적혀 있고, 실제로 Support·Ranking 어디에도 안 나온다(테스트가 확인한다). "이름·학교·전화번호 쓰지 말 것" 한 줄이 붙어 있다.
- **채팅이 아니다.** 플레이어끼리 서로의 글을 읽는 면이 0이라는 것이 이 설계의 전부다. 모더레이터 없는 청소년 서비스에 피드를 열 수 없고(6절), 정적 사이트라 애초에 못 만든다. **이 탭에 "다른 사람 글 보기"를 붙이자는 제안은 규칙 변경 결정 없이는 받지 말 것.**
- **집계 숫자는 자리표시자다.** `pollCounts()`가 도시 코드로 시드를 만들어 고정된 가짜 수를 낸다. 화면에 "stand-in until the server is connected"라고 **적어뒀다** — 지어낸 수를 진짜 집계처럼 보이면 심사에서 그것 하나로 무너진다. `pollPct()`가 반올림 오차를 최대 항목에 몰아서 합이 항상 정확히 100이 되게 한다.
- **실제 전송은 `sendNote()` 한 함수 안에만 있다.** 광고의 `watchAd()`와 같은 구조다. 구글 폼 주소가 정해지면 거기만 바꾼다. 미성년자 응답을 받는 폼이므로 이름·학교·연락처 칸은 두지 않는다.
- **투표에 코인 보상은 없다.** 코인은 탭(=배출 감축)에서만 나온다. 의견에 코인을 주면 "탭 = 감축 = 코인"이 깨진다.
- 미응답이면 탭에 **노란 점**이 붙는다(빨간 점은 파도 전용이라 색을 나눴고, 깜빡이지 않는다).
- **입력칸은 절대 다시 그리지 않는다.** `#voice`의 뼈대는 정적 HTML이고 `renderVoice()`는 투표 영역만 건드린다. `renderLists`에서 Voice만 `worldStamp`가 빠진 별도 서명을 쓰는 것도 같은 이유다 — 어딘가에 파도가 치면 서명이 움직이는데, 그때 목록을 다시 그리면 **쓰던 문장이 날아간다.** 테스트가 이걸 지킨다.

### 그 밖에

- Ranking: 1인당 감축 + **쌓은 방어** + 준 코인. 방어를 안 세면 방재 도시는 영구 꼴찌라서 넣었다.
- 스모그 농도는 그 도시의 **절대 배출량** 기준이다(속초 0.10 / 울산 0.90). 예전엔 자기 하루치의 비율이라 어촌도 공업도시처럼 새까맸다.
- 탭 25회마다 재난 상식 한 줄(FACTS 배열).
- 노트북: 스페이스바 = 탭(My City에서만, 누르고 있어도 1회).
- localStorage 저장(키 "yl"), 기기 UID 난수. 옛 저장 데이터는 새 필드가 기본값으로 채워져 그대로 열린다.

### 안 되는 것 (전부 가짜)

- 순위·다른 도시 데이터는 클라이언트 난수. 서버 없음
- Voice 투표 집계도 시드 난수(화면에 자리표시자라고 명시). 제출한 의견은 기기 밖으로 안 나감
- 나에게 오는 구호(`inbound()`)도 시뮬레이션. 진짜 다른 플레이어가 보낸 게 아니다
- 광고는 자리표시자 화면. 실제 광고망 미연결
- IP 지역 감지 없음(정적 사이트라 불가). 시간대 추정 코드는 글로벌 모드용으로 남겨둠
- 기부·장부는 숫자만 올라감

### 손대기 전에 알아야 할 함정

- **탭 중에 목록을 다시 그리면 안 된다.** 손가락이 누르고 있던 버튼이 교체되면 브라우저가 클릭을 부모로 흘려서 탭이 씹힌다. 싸구려 안드로이드 폰에서 바로 티가 난다. 아래 "목록을 다시 그리지 않는다" 참고.
- 날짜는 UTC가 아니라 기기 로컬 기준(`today()`). UTC로 하면 한국에서 오전 9시에 날짜가 넘어가 스트릭이 안 늘었다.
- `tick()`은 1초가 아니라 **실제 경과 시간**을 지급한다. 화면이 꺼져 있으면 타이머가 멈추기 때문이다.
- localStorage 쓰기는 동기라서 탭마다 쓰면 끊긴다. 모아 썼다가 백그라운드 진입·탭 종료 때 반드시 flush한다.
- id 중복 주의. `<section id="city">`와 SVG의 `<g id="city">`가 충돌했던 적이 있다(지금은 `cityArt`).

## 6. 데이터·정치 주의

- `pop`(인구, 천 명)도 `role`·`base`와 같은 임시값이다. 점수에는 안 쓰이지만 표에 있으니 교체할 때 같이 한다. 출처·연도는 README의 표에 적는다.
- 도시/국가 역할 라벨은 공개 데이터 근거로 붙인다: 배출은 Global Carbon Project(국가) 또는 온실가스종합정보센터(한국 지자체), 위험은 World Risk Index 또는 행안부 재해 통계. 코드의 base 값과 역할은 현재 임시값이다. 실제 수치로 교체할 때 근거 연도를 README에 적을 것.
- UI에 특정 국가를 "오염국"으로 지목하는 문구 금지. 국기·국경 지도 사용 금지(표기 논쟁 회피). 국가명과 색만.
- 업그레이드 이름(`LOCAL`)은 지명·산업·지형만 쓴다. **기업명 금지** — 포항은 POSCO가 아니라 steelworks, 울산은 현대가 아니라 refinery/complex다. **바다 이름도 안 쓴다** — 인천 해상풍력에 서해·황해를 넣지 않았다. 같은 표기 논쟁을 피하려는 것이다. 만·강·항구 이름은 괜찮다.
- 방재 수단은 그 지역에 실제로 있는 것만 쓴다. 기본값이던 "Mangrove belt"는 한국에 맹그로브가 안 자라서 도시별 방재림·갯벌·사구로 바꿨다(강릉=해안 곰솔림, 목포=갯벌, 제주=돌담+사구). 글로벌 모드 폴백에는 맹그로브가 남아 있다 — 거기선 맞는 말이라서.
- 로그인·이름·GPS 수집 금지. 채팅 기능 금지(청소년 서비스). **Voice 탭은 이 규칙의 예외가 아니다** — 플레이어가 쓴 글을 다른 플레이어가 읽는 면이 하나도 없어서 채팅이 아닌 것이다. 신고 버튼·욕설 필터·매일 글을 읽는 사람이 준비되기 전에는 읽기 면을 열지 않는다.
- "광고 수익을 기부한다"는 약속은 심사·공개에서 검증받는다. 실제 수익은 초기에 몇 달러 수준임을 인정하고, 금액보다 "기부가 실제로 1건 일어났다"와 "공개 장부"를 목표로 말할 것.

## 7. 다음 할 일 (우선순위 순)

0. **폰에서 직접 확인** — 이번 변경(역할별 모드, My City 통합, 재난 3분 카드, 광고 화면, 도시별 풍경)은 **실제 폰에서 아직 한 번도 안 돌려봤다.** 자동 테스트는 통과했지만 그건 레이아웃을 못 본다. 특히 볼 것: 상단바에 도시명+코인이 380px에서 안 깨지는지, 미터 카드 두 장이 좁은 화면에서 눌리지 않는지, 방어 모드에서 벽에 블록 떨어지는 게 보이는지, 3분 카운트다운 중에 탭이 씹히지 않는지, Support 줄의 버튼 두 개(코인/▶ Ad)가 380px에서 안 깨지는지, 풍경 애니메이션 때문에 싼 폰에서 탭이 느려지지 않는지. 확인 후 1~3번 변경 커밋.
1. **GitHub Pages 배포** — README 절차대로. 링크 하나가 나오면 그게 발표 시연 링크.
2. **도시 데이터 실제값** — 한국 12개 도시의 역할·base를 공개 통계로 교체(6절). 업그레이드 이름은 지명·산업 기준이라 그대로 두면 된다.
3. **게임 재미 손질** — 지루한 지점: 초반 코인 모으는 속도, 파도 이벤트 빈도, 업그레이드 가격 곡선. 학교 테스트 후 조정. 파도 45초 / 비상 3분이라 비상 카드가 거의 항상 떠 있을 수 있다 — 실제로 지루한지 폰에서 보고 판단할 것.
5. **광고 연결 설계** — 자리표시자 화면은 완성됨. `watchAd()` 안에 실제 광고망을 붙일 것. 정적 사이트에서 가능한 리워드 광고(예: Google AdSense/AdMob 웹 리워드 광고) 조사. 미성년 개발자 계정 문제는 NGO 또는 보호자 명의로 해결해야 할 수 있음.
6. **최소 백엔드 (선택)** — 진짜 순위를 원하면 필요. 후보: Cloudflare Workers + KV, 또는 Supabase. 기능: 도시별 합산, 하루 1회 상한 서버 검증, IP → 지역. 정적 폴백은 유지.
7. **공개 장부** — 구글 시트 공개 링크를 Ranking 탭 장부 카드에 연결.
8. **Global Click Day 준비물** — 참가 학교용 안내문 1장 + QR, NGO 서면 합의 1장, 당일 진행자 2명.
9. **글로벌 모드 전환** — `const COUNTRIES = REGIONS_KOREA;`를 `COUNTRIES_GLOBAL`로 바꾸면 됨. 시간대 추정 코드가 다시 살아남.

## 8. 파일

- `index.html` — 마크업(154줄). **head의 배포 주소 주석을 지우지 말 것.**. `styles.css`와 `app.js`를 상대경로로 부른다.
- `styles.css` — 스타일(171줄)
- `app.js` — 로직(763줄). **일반 스크립트다. `type="module"`로 바꾸지 말 것** — 목록의 `onclick=` 핸들러가 전부 죽는다.
- `README.md` — 실행·배포 방법
- `CLAUDE.md` — Claude Code용 작업 규칙
- `HANDOVER.md` — 이 문서
- `icon.svg`, `icon-180/192/512.png` — 파비콘, iOS·안드로이드 홈 화면. `icon.svg`를 고치면 PNG도 다시 렌더해야 한다(0절의 헤드리스 방법으로 `--window-size=N,N`).
- `og.png` — 카톡·슬랙이 링크에 보여주는 1200×630 그림
- `manifest.webmanifest` — 홈 화면 추가
- `.nojekyll` — Pages가 파일을 그대로 서빙하게 함
- `test/` — 개발용 회귀 테스트(jsdom). `npm install` 후 `npm test`. 493개.
  **손으로 확인하는 것을 대신하지 않는다.** CLAUDE.md의 절차(탭 30번, 업그레이드 구매, 코인 송금, 랭킹 확인)는 그대로 해야 한다. 12개 도시를 매번 다 눌러볼 수 없어서 회귀만 기계에 맡긴 것이다. 앱과 배포에는 영향이 없고, 통째로 지워도 된다. 자세한 건 `test/README.md`.
- `package.json`, `.gitignore` — 위 테스트용. 앱은 안 쓴다.

관련 산출물(저장소 밖): Attachment_4-Presentation_Overview_KoreaTeam.docx(제출 서류), youth-leader-slides.pptx(발표 슬라이드 08–19).
